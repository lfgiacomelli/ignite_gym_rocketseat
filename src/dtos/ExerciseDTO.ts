export type ExerciseDTO = {
    thumbnail: string | undefined
    id: string,
    demo: string,
    group: string,
    name: string,
    repetitions: number,
    series: number,
    thumb: string,
    updated_at: string
}